name: blank

on:
  push:

jobs:
  ancient:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        county: ["fifteen", "social", "revolutionary", "pork", "amusing"]
        annually: ["liner", "indication", "case", "predictable"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Rt
